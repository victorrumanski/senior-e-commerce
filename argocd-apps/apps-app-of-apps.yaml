apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: apps-app-of-apps
  namespace: argocd
spec:
  project: default
  source:
    # GitOps repo (this repository)
    # Update this URL after you publish your repo.
    repoURL: https://github.com/victorrumanski/senior-e-commerce.git
    targetRevision: HEAD
    path: argocd-apps
    directory:
      recurse: true
      include: 'apps-*.yaml'
      exclude: '{apps-app-of-apps.yaml,infra-*.yaml,observability-*.yaml,my-spring-service.yaml,infra-app-of-apps.yaml}'
  destination:
    server: https://kubernetes.default.svc
    namespace: argocd
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
