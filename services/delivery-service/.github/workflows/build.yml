name: Delivery Service CI

on:
  push:
    branches: [ "main" ]
    paths:
      - "services/delivery-service/**"
      - ".github/workflows/reusable-service-build.yml"
  pull_request:
    branches: [ "main" ]
    paths:
      - "services/delivery-service/**"

permissions:
  contents: read
  packages: write

jobs:
  build:
    uses: ./.github/workflows/reusable-service-build.yml
    with:
      service_path: services/delivery-service
      service_name: delivery-service
