name: Checkout Service CI

on:
  push:
    branches: [ "main" ]
    paths:
      - "services/checkout-service/**"
      - ".github/workflows/reusable-service-build.yml"
  pull_request:
    branches: [ "main" ]
    paths:
      - "services/checkout-service/**"

permissions:
  contents: read
  packages: write

jobs:
  build:
    uses: ./.github/workflows/reusable-service-build.yml
    with:
      service_path: services/checkout-service
      service_name: checkout-service
